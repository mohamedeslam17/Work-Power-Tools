# Audit corpus (temporary)

Drop lab report `.xlsx` files here for Claude to batch-audit and tune the tool.
This branch is **not** merged into `main` and gets deleted after the audit, so
the reports never bloat the deployed repo.

Run locally: `python3 batch_review.py audit_reports`
